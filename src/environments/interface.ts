export interface Environment {
    apiKey: string;
    production: boolean;
    fbDbUrl: string;
    rovraggeUrl?: string;
    rovraggePlacesUrl?: string;
    placeUrl?: string;
    articleUrl?: string;
}